# GA local verification — 2026-05-27

Environment: developer machine (not staging). See [ga-signoff.md](../../docs/ga-signoff.md) for staging gates (G-01, G-02).

## Automated (repo)

| Step | Command | Result |
|------|---------|--------|
| Unit/integration | `dotnet test 33pol.sln -c Release` | **PASS** (583 tests) |
| Coverage gates | `build/check-coverage.sh` | **PASS** (Billing 98.9%, Registry 91.6%, Proxy 91.6%, Security 90.0%, Policy 93.4%, Observability 93.3%) |

## k6

| Script | Command | Result | Notes |
|--------|---------|--------|-------|
| `smoke.js` | `bash perf/ci/run-smoke.sh` | **PASS** | p95 &lt; 500ms, 0% errors; local gateway + Python mock upstream |
| GA suite (Compose, short) | `bash perf/ci/run-ga-compose-k6.sh` | **PASS** | Local substitute for G-01 when no staging URL; uses `mock-gpt` and auto-generated inference key; not production thresholds. |

**Staging GA suite (G-01):** not run — no staging URL configured.

**4h soak (G-02):** not run — staging only per plan.

Local substitute soak: `SOAK_DURATION=3m bash perf/ci/run-soak-local.sh` — **PASS** (VUs=1, sleep=5s).

## Docker Compose (G-04)

| Step | Command | Result |
|------|---------|--------|
| Stack | `docker compose up -d` (repo root) | **Running** (postgres, mock-upstream, prometheus, grafana, gateway) |
| Health | `bash perf/ci/verify-compose-health.sh` | **PASS** |
| E2E | `bash perf/ci/run-compose-e2e.sh` | **PASS** (2026-05-27, `MODEL=mock-gpt`) |

Compose fixes: WireMock `GET /health` mapping; SSE stream mapping for `stream:true`; `mock-gpt` restored in `deploy/docker/config/models.json`.

## OpenAI Python SDK (`perf/scripts/sdk-smoke.py`) — G-03

| Step | Result |
|------|--------|
| `GET /v1/models` | **PASS** (`mock-gpt`) |
| Chat (non-stream) | **PASS** |
| Chat (stream) | **PASS** | After WireMock SSE mapping + `GET /health` probe (2026-05-27) |

Env used:

```bash
export OPENAI_BASE_URL=http://127.0.0.1:8080/v1
export OPENAI_API_KEY=<inference key from POST /admin/api/keys>
export MODEL=mock-gpt
```

## G-07 registry poll (local Compose)

| Step | Result |
|------|--------|
| Edit `deploy/docker/config/models.json` (add alias `poll-test-alias` on `mock-gpt`) | **PASS** |
| `POST /v1/chat/completions` with `model=poll-test-alias` within ~3s | **PASS** (routes to mock-gpt) |

## G-08 FinOps export (local)

| Step | Result |
|------|--------|
| `GET /admin/api/usage/export?format=csv` | **PASS** (header row; empty usage acceptable on fresh DB) |

## G-09 operator console

Deferred: `Gateway__OperatorConsole__Enabled=false` in Compose; use `/admin` UI and Grafana per [ga-signoff.md](../../docs/ga-signoff.md).

## G-06 observability (local substitute)

Metrics path verified on Compose:

- `bash perf/ci/verify-observability-local.sh` — **PASS** (`/metrics`, Prometheus targets up, Grafana healthy)

Traces are not exported by the host in this local setup (metrics-only OTel). See [docs/ga-signoff-no-staging.md](../../docs/ga-signoff-no-staging.md) for the documented waiver.

## Still required for GA sign-off (staging / process)

- G-01: `k6-ga-staging.yml` on staging with production thresholds  
- G-02: 4h `soak.js` on staging  
- G-03: Re-run SDK smoke on **staging** (local Compose passed)  
- G-05: Checklist approvals (Engineering / Ops / Product names)  
- G-06: OTel traces end-to-end on staging  

## Fixes applied during verification

- `DailyUsageWebhookPublisherTests`: align `yesterday` with test `utcNow` (flaky on calendar day boundary).  
- `perf/ci/verify-compose-health.sh`: use `docker compose ps --services --filter status=running` (pipefail-safe).  
- `perf/ci/run-ga-local.sh`: remove `--max-vus` (k6 v2 incompatible).  
- `perf/ci/run-compose-e2e.sh`: formal Compose E2E script (G-04).  
- WireMock: `health.json`, `chat-completions-stream.json`; non-stream body `doesNotContain` stream flag.  
- `deploy/docker/config/models.json`: `mock-gpt` → `http://mock-upstream:8080` for reproducible Compose GA.
