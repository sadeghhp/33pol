# GA performance report — 2026-05-26

**Environment:** local-perf (mock upstream + gateway on developer machine)  
**Sign-off:** Staging numbers required before GA release (use `.github/workflows/k6-ga-staging.yml`).

## Scripts executed

| Script | CI / local | Result |
|--------|------------|--------|
| `smoke.js` | CI `k6-smoke` on `main` | Pass (30s in CI) |
| `inference-rps.js` | `perf/ci/run-ga-local.sh` (shortened) | Pass locally |
| `streaming-concurrent.js` | `run-ga-local.sh` | Pass locally |
| `rate-limit-storm.js` | `run-ga-local.sh` | Pass locally |
| `soak.js` | Manual staging (default 4h) | Pending staging |
| `overhead-compare.js` | `perf/ci/run-overhead-compare.sh` | Run after mock + gateway up |

## Gateway overhead (local-perf)

| Metric | Direct mock p99 | Via gateway p99 | Delta |
|--------|-----------------|-----------------|-------|
| `http_req_duration` | _fill from k6 summary_ | _fill from k6 summary_ | Target &lt; 5 ms |

Methodology: `perf/k6/scripts/overhead-compare.js` with `DIRECT_URL` → mock, `GATEWAY_URL` → gateway.

## Staging gates (required for GA)

- [ ] `inference-rps.js` thresholds on staging (`p(99)<60000`, `http_req_failed < 1%`)
- [ ] `streaming-concurrent.js` at target concurrency (50–500 VUs per environment capacity)
- [ ] `soak.js` 4h at 70% max RPS — memory stable
- [ ] Grafana snapshot archived under `perf/reports/`

## Notes

- Operator console overhead (optional): compare load with `Gateway:OperatorConsole:Enabled` true vs false; target ≤ 1 ms p99 delta on local-perf.
- Real vLLM staging run recommended before production sign-off (mock is optimistic for TTFT).
