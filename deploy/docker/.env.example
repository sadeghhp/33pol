# Copy to deploy/docker/.env and adjust as needed.
# cp .env.example .env

POSTGRES_USER=gateway
POSTGRES_PASSWORD=gateway
POSTGRES_DB=gateway
POSTGRES_PORT=5432

GATEWAY_PORT=8080
GATEWAY_ADMIN_API_KEY=sk-33pol-dev-admin-key
MOCK_UPSTREAM_PORT=18080

PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

ASPNETCORE_ENVIRONMENT=Development

# Optional: OpenRouter provider token (used by models with upstreamAuth.envVar=OPENROUTER_API_KEY)
# OPENROUTER_API_KEY=sk-or-...
