apiVersion: v1
kind: Service
metadata:
  name: {{ include "33pol.fullname" . }}
  labels:
    {{- include "33pol.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "33pol.selectorLabels" . | nindent 4 }}
