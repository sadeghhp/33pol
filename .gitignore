# Local Compose overrides
deploy/docker/.env

# Build artifacts
**/bin/**
**/obj/**
TestResults/

# Local dev secrets (Postgres connection string)
src/33pol.App/appsettings.Development.local.json

# Local Compose overrides
deploy/docker/.env
TestResults-*
perf/*
.env

# Operator config (never commit registry or encrypted upstream keys)
deploy/docker/config/models.json
deploy/docker/config/models.json.local
deploy/docker/config/upstream-secrets.enc
deploy/docker/config/upstream-secrets.enc.local

# Secret audit artifacts (local only)
gitleaks-report.json
trufflehog-report.json
audit-refs.txt
audit-summary.txt
gitleaks-stdout.txt
trufflehog-stderr.txt
