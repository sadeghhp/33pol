# All-in-one 33pol local stack (gateway with embedded SQLite + mock upstream + Prometheus + Grafana).
# From repo root:
#   cp .env.example .env
#   docker compose up -d --build
#
# Same services as deploy/docker/docker-compose.yml (included for a single entry point).

include:
  - deploy/docker/docker-compose.yml
